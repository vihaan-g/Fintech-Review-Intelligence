"""Agent-style components that consume pipeline outputs.

Currently hosts InsightReporter, which generates the final markdown
artifacts from CouncilResult + FindingsSummary. No external API calls.
"""
